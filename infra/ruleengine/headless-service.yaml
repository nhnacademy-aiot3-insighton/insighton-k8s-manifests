apiVersion: v1
kind: Service
metadata:
  name: insighton-ruleengine-headless
  namespace: insighton
spec:
  clusterIP: None
  selector:
    app: insighton-ruleengine
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
