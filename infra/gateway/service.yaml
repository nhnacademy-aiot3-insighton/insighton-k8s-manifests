apiVersion: v1
kind: Service
metadata:
  name: infra-gateway
  namespace: insighton
spec:
  selector:
    app: infra-gateway
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP