apiVersion: v1
kind: Service
metadata:
  name: insighton-core
  namespace: insighton
spec:
  selector:
    app: insighton-core
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
