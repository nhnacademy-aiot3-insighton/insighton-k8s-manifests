apiVersion: v1
kind: Service
metadata:
  name: insighton-core-headless
  namespace: insighton
spec:
  clusterIP: None
  selector:
    app: insighton-core
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080