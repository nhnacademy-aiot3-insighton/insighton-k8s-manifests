apiVersion: v1
kind: Service
metadata:
  name: insighton-front
  namespace: insighton
spec:
  selector:
    app: insighton-front
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
