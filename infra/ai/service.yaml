apiVersion: v1
kind: Service
metadata:
  name: insighton-ai
  namespace: insighton
spec:
  selector:
    app: insighton-ai
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
