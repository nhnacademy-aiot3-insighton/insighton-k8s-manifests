apiVersion: v1
kind: Service
metadata:
  name: insighton-config
  namespace: insighton
spec:
  selector:
    app: insighton-config
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
