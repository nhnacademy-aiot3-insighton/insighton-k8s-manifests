apiVersion: v1
kind: Service
metadata:
  name: insighton-actuator-simulator
  namespace: insighton
spec:
  selector:
    app: insighton-actuator-simulator
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
